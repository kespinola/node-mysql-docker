{
  "name": "prism-server",
  "version": "0.0.1",
  "description": "prism server instance.",
  "main": "index.js",
  "scripts": {
    "migrate:seed": "npm run migrate; npm run seed",
    "migrate": "knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "seed": "knex seed:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "prism inc.",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.3.4",
    "dotenv": "^2.0.0",
    "electrolyte": "^0.2.0",
    "jsonwebtoken": "^5.7.0",
    "knex": "^0.10.0",
    "mysql": "^2.10.2"
  },
  "devDependencies": {
    "babel-core": "^6.7.2",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^2.4.0",
    "eslint-config-airbnb": "^6.1.0",
    "mocha": "^2.4.5",
    "sinon": "^1.17.3"
  }
}
